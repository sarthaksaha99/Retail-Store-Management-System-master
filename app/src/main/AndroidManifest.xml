<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blackshadow.retailstoremanagementsystem">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RetailStoreManagementSystem">
        <activity android:name=".managerListActivity"></activity>
        <activity android:name=".mManageActivity" />
        <activity android:name=".purchasedProductDetailsActivity" />
        <activity android:name=".cartProductActivity" />
        <activity android:name=".paidProductActivity" />
        <activity android:name=".customerDetailsActivity" />
        <activity android:name=".customerTrakingActivity" />
        <activity android:name=".mproductDetailsActivity" />
        <activity android:name=".mProductActivity" />
        <activity android:name=".paymentActivity" />
        <activity android:name=".productDetailsActivity" />
        <activity android:name=".newProductActivity" />
        <activity android:name=".productActivity" />
        <activity android:name=".paybillActivity" />
        <activity android:name=".historyActivity" />
        <activity android:name=".profileActivity" />
        <activity android:name=".settingsActivity" />
        <activity android:name=".registrationActivity" />
        <activity android:name=".loginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>